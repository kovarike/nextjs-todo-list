generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Item {
  id        String  @id @unique @default(uuid())
  name      String
  completed Boolean @default(false) 
}
